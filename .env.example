# URL del backend de ofertas (Serverless / scraping).
# Ejemplos:
#   VITE_API_URL=https://tu-api.example.com
#   VITE_API_URL=http://localhost:8080   (backend local en server/index.mjs)
# Si se deja vacío, la app usa el set de ofertas mock locales.
VITE_API_URL=

